# Dreambit engine content

This directory owns the effects and fonts required by the Dreambit runtime.
Host applications import `BuildContent.targets` to compile these assets into
their own `Content` output directory without duplicating the source files.
