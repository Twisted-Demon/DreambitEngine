# Dreambit example content

This directory contains assets used only by `Dreambit.Examples`. The content
build generates `content.pak` from supported source assets and copies loose UI
layout XML files into the example application's `Content` output directory.
